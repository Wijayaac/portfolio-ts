import React, { ReactElement } from "react";
import Layout from "../../components/Layout";

interface Props {}

export default function Project({}: Props): ReactElement {
  return (
    <Layout>
      <h2>hello from the projects</h2>
    </Layout>
  );
}
